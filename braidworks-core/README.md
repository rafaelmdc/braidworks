# braidworks-core

Core abstractions for Braidworks: typed `Strand`s, `Weaver` capabilities, the
`BraidRegistry`, the `Braider` planner, and the `LocalExecutor`. Pure stdlib plus
`networkx`. No real weavers live here.
